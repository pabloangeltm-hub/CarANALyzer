# Agartha Frontend

React + TypeScript + Vite dashboard client for the Agartha B2B API.

## Scripts

```bash
npm run dev
npm run build
npm run lint
```

Set `VITE_API_BASE_URL` in `.env` when the FastAPI server is not running on `http://localhost:8000`.
